# Rich Text Editor (RTE)

rich text editor or RTE is software components allow user to manage thir text like world in the Microsoft and today in this we
video we are gonna create exact thing in the nextjs. before start turn on you computer and let go for coding

we use `tiptap` for create RTE rather than coding created from scratch tiptap is has free and prince but it's has neccessry features
for the RTE in the free plan so open the [tiptap](https://tiptap.dev/) go the `docs` > `Getting started` > choose `your language`
but in this video we are using nextjs

## 1 Create project (optional )

if you have nextjs project skip this part and go to the next one

```javascript
# create a project
npx create-next-app my-tiptap-project

# change directory
cd my-tiptap-project
```

## 2. Install the dependencies

```javascript
npm install @tiptap/react @tiptap/pm @tiptap/starter-kit
```

## 3. Create a new component

create new components called `Tiptap.js` for your editor

```javascript
"use client";

import { useEditor, EditorContent } from "@tiptap/react";
import StarterKit from "@tiptap/starter-kit";

const Tiptap = () => {
  const editor = useEditor({
    extensions: [StarterKit],
    content: "<p>Hello World! 🌎️</p>",
  });

  return <EditorContent editor={editor} />;
};

export default Tiptap;
```

## 4. Adding functionality

let adding extension like bold , italic , img into our own text editor
open the extension sections chose what ever you want for example bold

## 5. Installation of extension

```javascript
npm install @tiptap/extension-bold
```

if you scroll down you can see the keyword shortcut for your extension

## 6. Add extension to your project

### Step 1 add extension to project

add your extension at the top

```javascript

import Bold from "@tiptap/extension-bold";

```

### Step 2 Call it in your extension

now add your extension into list of the extension for your text editor

```javascript
const editor = useEditor({
  extensions: [Bold],
});
```

### Step 3 Add that into your component like this

```javascript
<button
  onClick={() => editor.chain().focus().toggleBold().run()}
  className={editor.isActive("bold") ? "is-active" : ""}
>
  toggleBold
</button>
```

now you can addd your own style for customize it I hope you can create exact thing into your project if you have any question just 
contact me 
